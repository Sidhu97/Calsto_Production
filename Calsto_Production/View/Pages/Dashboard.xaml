<Page
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:control="clr-namespace:FilterDataGrid;assembly=FilterDataGrid"
      xmlns:av="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="av" x:Class="Dashboard"
      Title="Dashboard" Background="#FAFAFA" FontFamily="Segoe UI" FontSize="11" >

    <Page.Resources>
        <!-- Header style -->
        <Style x:Key="HeaderStyle" TargetType="DataGridColumnHeader">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Background" Value="#F5F7FA"/>
            <Setter Property="Foreground" Value="#333"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="#DBE2E8"/>
        </Style>

        <!-- Row style without unconditional background -->
        <Style x:Key="RowStyle" TargetType="DataGridRow">
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Foreground" Value="#222"/>
            <Setter Property="MinHeight" Value="22"/>
            <Setter Property="Padding" Value="6,2"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="LightBlue"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="LightBlue"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Cell text style with trimming -->
        <Style x:Key="CellTextStyle" TargetType="TextBlock">
            <Setter Property="Padding" Value="6,0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
            <Setter Property="Foreground" Value="#444"/>
        </Style>

        <!-- Highlight blue for ProjectStatus Open -->
        <Style x:Key="ProjectStatusStyle" TargetType="TextBlock" BasedOn="{StaticResource CellTextStyle}">
            <Style.Triggers>
                <DataTrigger Binding="{Binding Project_Status}" Value="OPEN">
                    <Setter Property="Foreground" Value="DarkCyan"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Project_Status}" Value="HOLD">
                    <Setter Property="Foreground" Value="RED"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Project_Status}" Value="CLOSED">
                    <Setter Property="Foreground" Value="PURPLE"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </Page.Resources>

    <Grid Margin="5,5,0,0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="5*"/>
            <ColumnDefinition Width="5"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <control:FilterDataGrid  x:Name="DashDG" AutoGenerateColumns="False"
                                 SelectionChanged="dgProjects_Sel_Changed"
                                 ShowRowsCount="True"
                                 ShowStatusBar="True"
                                 FilterLanguage="English"
                                 RowHeight="22"
                                 FontSize="11"
                                 ColumnHeaderHeight="28"
                                 CanUserAddRows="False"
                                 Margin="0,0,0,0"
                                 AlternationCount="2"
                                 RowBackground="White"
                                 AlternatingRowBackground="#F8F9FB"
                                 CanUserResizeColumns="True"
                                 CanUserSortColumns="True"
                                 Grid.Column="0">
            <control:FilterDataGrid.Columns>
                <control:DataGridTextColumn Header="BOM No." Binding="{Binding BOM_No}" Width="80" IsColumnFiltered="True">
                    <control:DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock" BasedOn="{StaticResource CellTextStyle}">
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="Foreground" Value="Black"/>
                        </Style>
                    </control:DataGridTextColumn.ElementStyle>
                </control:DataGridTextColumn>
                <control:DataGridTextColumn Header="Customer" Binding="{Binding Customer_Name}" Width="350" IsColumnFiltered="True" ElementStyle="{StaticResource CellTextStyle}" />
                <control:DataGridTextColumn Header="Location" Binding="{Binding Location}" Width="110" IsColumnFiltered="True" ElementStyle="{StaticResource CellTextStyle}" />
                <control:DataGridTextColumn Header="PO No" Binding="{Binding PO_No}" Width="110" IsColumnFiltered="True" ElementStyle="{StaticResource CellTextStyle}" />
                <control:DataGridTextColumn Header="Booking Date" Binding="{Binding Booking_Date, StringFormat=\{0:dd-MM-yyyy\}}" Width="80" IsColumnFiltered="True" ElementStyle="{StaticResource CellTextStyle}" />
                <control:DataGridTextColumn Header="Sales" Binding="{Binding Sales}" Width="100" IsColumnFiltered="True" ElementStyle="{StaticResource CellTextStyle}" />
                <control:DataGridTextColumn Header="Total WIDs" Binding="{Binding Total_WIDs}" Width="80" IsColumnFiltered="True" ElementStyle="{StaticResource CellTextStyle}" />
                <control:DataGridTextColumn Header="Pending WIDs" Binding="{Binding Pending_WIDs}" Width="90" IsColumnFiltered="True" ElementStyle="{StaticResource CellTextStyle}" />
                <control:DataGridTextColumn Header="PO Dispatch Date" Binding="{Binding PO_Dispatch_Date, StringFormat=\{0:dd-MM-yyyy\}}" Width="100" IsColumnFiltered="True" ElementStyle="{StaticResource CellTextStyle}" />
                <control:DataGridTextColumn Header="Dispatch Date" Binding="{Binding Dispatch_Date, StringFormat=\{0:dd-MM-yyyy\}}" Width="100" IsColumnFiltered="True" ElementStyle="{StaticResource CellTextStyle}" />
                <control:DataGridTextColumn Header="Project Status" Binding="{Binding Project_Status}" Width="100" IsColumnFiltered="True" ElementStyle="{StaticResource ProjectStatusStyle}" />
                <control:DataGridTextColumn Header="Due Date" Binding="{Binding Due_Date}" Width="80" IsColumnFiltered="True" ElementStyle="{StaticResource CellTextStyle}" />
            </control:FilterDataGrid.Columns>
        </control:FilterDataGrid>

        <GridSplitter Grid.Column="1" Width="10" Background="#ddd" HorizontalAlignment="Center" VerticalAlignment="Stretch" />

        <!-- Details and StockIn Panel -->
        <Grid Grid.Column="2" Margin="0">
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition Height="2*"/>
            </Grid.RowDefinitions>

            <!-- Details DataGrid -->
            <DataGrid x:Name="SideDG"
                      AutoGenerateColumns="False" 
                      CanUserAddRows="False" 
                      IsReadOnly="True" 
                      FontSize="11" 
                      RowHeight="22" 
                      ColumnHeaderHeight="28" 
                      AlternatingRowBackground="#F8F9FB" 
                      HeadersVisibility="Column" 
                      Margin="0,0,0,12"
                      Grid.Row="1">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Field" Binding="{Binding Field}" Width="*" />
                    <DataGridTextColumn Header="Description" Binding="{Binding Detail}" Width="2*" />
                </DataGrid.Columns>
            </DataGrid>

            <!-- Stock IN Transaction Panel -->
            <Border Background="White" CornerRadius="6" Padding="16" BorderBrush="#D1D9E6" BorderThickness="1" Grid.Row="0">
                <StackPanel>
                    <TextBlock Text="Project Details" FontWeight="SemiBold" FontSize="13" Foreground="#222" Margin="0,0,0,12" />

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="110"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>



                        <TextBlock Text="PO Date:" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0,0,8,8"/>
                        <DatePicker x:Name="dpDispatchDate" Grid.Row="2" Grid.Column="1" Width="150" FontSize="12" Margin="0,0,0,8" />

                        <Button Content="Apply" Grid.Row="4" Grid.Column="1" Width="80" Height="30" Margin="0,12,0,0" Background="#007AFF" Foreground="White" FontWeight="SemiBold" HorizontalAlignment="Left" BorderThickness="0" Cursor="Hand" Command= "{Binding POdateApplyCommand}" Click="Button_Click" />
                        <Button Content="Hold/Cancel" Grid.Row="4" Grid.Column="0" Width="80" Height="30" Margin="0,12,0,0" Background="red" Foreground="White" FontWeight="SemiBold" HorizontalAlignment="Left" BorderThickness="0" Cursor="Hand" />

                    </Grid>


                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Page>