<Page
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:control="clr-namespace:FilterDataGrid;assembly=FilterDataGrid"
      xmlns:av="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      mc:Ignorable="av" x:Class="Forming"
      Title="Forming" Background="#FAFAFA" 
    FontFamily="Segoe UI" 
    FontSize="11" 
    av:DesignHeight="594.701">

    <Page.Resources>


        <!-- Header style -->
        <Style x:Key="HeaderStyle" TargetType="{x:Type DataGridColumnHeader}">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Background" Value="#F5F7FA"/>
            <Setter Property="Foreground" Value="#333"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="#DBE2E8"/>
        </Style>

        <!-- Row style without unconditional background -->
        <Style x:Key="RowStyle" TargetType="{x:Type DataGridRow}">
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Foreground" Value="#222"/>
            <Setter Property="MinHeight" Value="22"/>
            <Setter Property="Padding" Value="6,2"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="LightBlue"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="LightBlue"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Cell text style with trimming -->
        <Style x:Key="CellTextStyle" TargetType="{x:Type TextBlock}">
            <Setter Property="Padding" Value="6,0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
            <Setter Property="Foreground" Value="#444"/>
        </Style>

        <!-- Highlight blue for ProjectStatus Open -->
        <Style x:Key="ProjectStatusStyle" TargetType="{x:Type TextBlock}" BasedOn="{StaticResource CellTextStyle}">
            <Style.Triggers>
                <DataTrigger Binding="{Binding ProjectStatus}" Value="OPEN">
                    <Setter Property="Foreground" Value="Green"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding ProjectStatus}" Value="HOLD">
                    <Setter Property="Foreground" Value="RED"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding ProjectStatus}" Value="CLOSED">
                    <Setter Property="Foreground" Value="PURPLE"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </Page.Resources>

    <Grid Margin="16">
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="10"/>
            <ColumnDefinition Width="3.2*"/>
        </Grid.ColumnDefinitions>



        <!-- Master Data Grid -->
        <control:FilterDataGrid 
            x:Name="ProjDG"
            AutoGenerateColumns="False"
            CanUserAddRows="False"
            SelectionChanged="FormProj_Sel_Changed"
            ShowRowsCount="True"
            ShowStatusBar="True"
            FilterLanguage="English"
            RowHeight="22"
            FontSize="11"
            ColumnHeaderHeight="28"
            Margin="0"
            AlternationCount="2"
            RowBackground="White"
            AlternatingRowBackground="#F8F9FB"
            ColumnHeaderStyle="{StaticResource HeaderStyle}"
            RowStyle="{StaticResource RowStyle}"
            CanUserResizeColumns="True"
            CanUserSortColumns="True"
            Grid.Column="0">
            <control:FilterDataGrid.Columns>
                <control:DataGridTextColumn Header="BOM No." Binding="{Binding PROJECTNO}" Width="110" IsColumnFiltered="True">
                    <control:DataGridTextColumn.ElementStyle>
                        <Style TargetType="{x:Type TextBlock}" BasedOn="{StaticResource CellTextStyle}">
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="Foreground" Value="Black"/>
                        </Style>
                    </control:DataGridTextColumn.ElementStyle>
                </control:DataGridTextColumn>

                <control:DataGridTextColumn Header="Customer" Binding="{Binding Customer}" Width="250" IsColumnFiltered="True" ElementStyle="{StaticResource CellTextStyle}" />

            </control:FilterDataGrid.Columns>
        </control:FilterDataGrid>

        <!-- Splitter -->
        <GridSplitter Grid.Column="1" Width="10" Background="#ddd" HorizontalAlignment="Center" VerticalAlignment="Stretch" />

        <!-- Details and StockIn Panel -->
        <Grid Grid.Column="2" Margin="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="20"/>
                <RowDefinition/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <DockPanel Grid.Row="0" HorizontalAlignment="Left">
                <!-- Details DataGrid -->
                <TextBlock Text="Project No:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" FontWeight="SemiBold" Margin="10,0,8,8"/>
                <TextBlock x:Name ="txt_Projectno" Grid.Row="0"  FontWeight="SemiBold" FontSize="15" Foreground="DarkCyan" Grid.Column="0" VerticalAlignment="Center"  Margin="0,0,8,8"/>
                <TextBlock Text="Customer Name:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" FontWeight="SemiBold" Margin="10,0,8,8"/>
                <TextBlock x:Name ="txt_Customer"  Grid.Row="0"  FontWeight="SemiBold" FontSize="15" Foreground="DarkCyan" Grid.Column="0" VerticalAlignment="Center"  Margin="0,0,8,8"/>
            </DockPanel>

            <!-- Details DataGrid -->
            <DockPanel Grid.Row="0" HorizontalAlignment="Right" >

                <CheckBox x:Name="SelectAllFilteredCheckBox" Content="Select all-Filtered" Width="130" VerticalAlignment="Center" />
                <Button Content="Create Job Card" Background="#007AFF" Foreground="White" FontWeight="SemiBold" BorderThickness="0" Cursor="Hand"  DockPanel.Dock="Right" Width="86" Margin="0,0,50,0" />

            </DockPanel>

            <!-- Details DataGrid -->
            <control:FilterDataGrid 
    x:Name="FormDG"
    SelectionMode="Single"
    AutoGenerateColumns="False" 
    CanUserAddRows="False"
    ShowRowsCount="True"
    ShowStatusBar="True"
    FilterLanguage="English"
    RowHeight="22"
    FontSize="11"
    ColumnHeaderHeight="28"
    AlternationCount="1"
    RowBackground="White"
    AlternatingRowBackground="#F8F9FB"
    ColumnHeaderStyle="{StaticResource HeaderStyle}"
    RowStyle="{StaticResource RowStyle}"
    CanUserResizeColumns="True"
    CanUserSortColumns="True"
    Margin="0,0,0,12" Grid.Row="1">

                <control:FilterDataGrid.Columns>
                    <control:DataGridTextColumn Header="Job Card" Binding="{Binding JC_no}" Width="0" IsColumnFiltered="True" ElementStyle="{StaticResource CellTextStyle}" />
                    <control:DataGridTextColumn Header="BOM Name" Binding="{Binding BOMName}" Width="130" IsColumnFiltered="True">
                        <control:DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock" BasedOn="{StaticResource CellTextStyle}">
                                <Setter Property="FontWeight" Value="Bold"/>
                                <Setter Property="Foreground" Value="Black"/>
                            </Style>
                        </control:DataGridTextColumn.ElementStyle>
                    </control:DataGridTextColumn>
                    <control:DataGridTextColumn Header="Part Number" Binding="{Binding PartNumber}" Width="140" IsColumnFiltered="True" ElementStyle="{StaticResource CellTextStyle}" />
                    <control:DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="280" IsColumnFiltered="True" ElementStyle="{StaticResource CellTextStyle}" />
                    <control:DataGridTextColumn Header="Length" Binding="{Binding Length}" Width="60" IsColumnFiltered="True" ElementStyle="{StaticResource CellTextStyle}" />
                    <control:DataGridTextColumn Header="Width" Binding="{Binding Width}" Width="60" IsColumnFiltered="True" ElementStyle="{StaticResource CellTextStyle}" />
                    <control:DataGridTextColumn Header="Thick" Binding="{Binding Thick}" Width="60" IsColumnFiltered="True" ElementStyle="{StaticResource CellTextStyle}" />
                    <control:DataGridTextColumn Header="Unit Weight" Binding="{Binding UnitWeight}" Width="80" IsColumnFiltered="True" ElementStyle="{StaticResource CellTextStyle}" />
                    <control:DataGridTextColumn Header="Total Weight" Binding="{Binding TotalWeight}" Width="110" IsColumnFiltered="True" ElementStyle="{StaticResource CellTextStyle}" />
                    <control:DataGridTextColumn Header="Colour" Binding="{Binding Colour}" Width="90" IsColumnFiltered="True" ElementStyle="{StaticResource CellTextStyle}" />
                    <control:DataGridTextColumn Header="Qnty" Binding="{Binding BOMQty}" Width="90" IsColumnFiltered="True" ElementStyle="{StaticResource CellTextStyle}" />
                    <control:DataGridTextColumn Header="Produced Qty" Binding="{Binding ProducedQty}" Width="90" IsColumnFiltered="True" ElementStyle="{StaticResource CellTextStyle}" />
                    <control:DataGridTextColumn Header="Balance Qty" Binding="{Binding BalanceQty}" Width="90" IsColumnFiltered="True" ElementStyle="{StaticResource CellTextStyle}" />
                </control:FilterDataGrid.Columns>
            </control:FilterDataGrid>
            <Grid x:Name="___Grid__3rd" Grid.Row="2">

                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition Width="Auto" MinWidth="150"/>

                </Grid.ColumnDefinitions>

                <control:FilterDataGrid 
                    x:Name="JobcardDG"
                   
                    AutoGenerateColumns="False"    
                    ShowRowsCount="True"
                    CanUserAddRows="False"
                    ShowStatusBar="True"
                    FilterLanguage="English"
                    RowHeight="22"
                    FontSize="11"
                    ColumnHeaderHeight="28"
                    AlternationCount="2"
                    RowBackground="White"
                    AlternatingRowBackground="#F8F9FB"
                    ColumnHeaderStyle="{StaticResource HeaderStyle}"
                    RowStyle="{StaticResource RowStyle}"
                    CanUserResizeColumns="True"
                    CanUserSortColumns="True" Margin="0,0,10,0">

                    <control:FilterDataGrid.Columns>
                        <control:DataGridTextColumn Header="Seq." Binding="{Binding OP_Sequence}" Width="60" IsColumnFiltered="True" ElementStyle="{StaticResource CellTextStyle}" />
                        <control:DataGridTextColumn Header="Job Card" Binding="{Binding JC_no}" Width="150" IsColumnFiltered="True" ElementStyle="{StaticResource CellTextStyle}" />
                        <control:DataGridTextColumn Header="WID" Binding="{Binding WID}" Width="100" IsColumnFiltered="True" ElementStyle="{StaticResource CellTextStyle}" />
                        <control:DataGridTextColumn Header="Operation" Binding="{Binding Operation_ID}" Width="100" IsColumnFiltered="True" ElementStyle="{StaticResource CellTextStyle}" />
                        <control:DataGridTextColumn Header="Created On" Binding="{Binding Created_date}" Width="100" IsColumnFiltered="True" ElementStyle="{StaticResource CellTextStyle}" />
                        <control:DataGridTextColumn Header="Created by" Binding="{Binding Created_by}" Width="100" IsColumnFiltered="True" ElementStyle="{StaticResource CellTextStyle}" />
                    </control:FilterDataGrid.Columns>
                </control:FilterDataGrid>




                <Border Background="White" CornerRadius="6" Padding="16" BorderBrush="#D1D9E6" BorderThickness="1" Margin="0,0,0,0" Grid.Column="1">
                    <StackPanel>
                        <TextBlock Text="Project Details" FontWeight="SemiBold" FontSize="13" Foreground="#222" Margin="0,0,0,12" />

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="110"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>



                            <TextBlock Text="PO Date:" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0,0,8,8"/>
                            <DatePicker Grid.Row="2" Grid.Column="1" Width="150" FontSize="12" Margin="0,0,0,8"/>

                            <TextBlock Text="PO Date:" Grid.Row="3" Grid.Column="0" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0,0,8,8"/>
                            <DatePicker Grid.Row="3" Grid.Column="1" Width="150" FontSize="12" Margin="0,0,0,8"/>

                            <Button Content="Apply" Grid.Row="4" Grid.Column="1" Width="80" Height="30" Margin="0,12,0,0" Background="#007AFF" Foreground="White" FontWeight="SemiBold" HorizontalAlignment="Left" BorderThickness="0" Cursor="Hand"  />
                            <Button Content="Delete Jobs" Grid.Row="4" Grid.Column="0" Width="80" Height="30" Margin="0,12,0,0" Background="red" Foreground="White" FontWeight="SemiBold" HorizontalAlignment="Left" BorderThickness="0" Cursor="Hand"/>

                        </Grid>


                    </StackPanel>
                </Border>
            </Grid>

        </Grid>
    </Grid>
</Page>
