<Page
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:control="clr-namespace:FilterDataGrid;assembly=FilterDataGrid"
      xmlns:av="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      mc:Ignorable="av" x:Class="Forming"
      Title="Forming" Background="#FAFAFA" 
    FontFamily="Segoe UI" 
    FontSize="11" 
    av:DesignHeight="594.701">

    <Page.Resources>


        <!-- Header style -->
        <Style x:Key="HeaderStyle" TargetType="{x:Type DataGridColumnHeader}">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Background" Value="#F5F7FA"/>
            <Setter Property="Foreground" Value="#333"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="#DBE2E8"/>
        </Style>

        <!-- Row style without unconditional background -->
        <Style x:Key="RowStyle" TargetType="{x:Type DataGridRow}">
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Foreground" Value="#222"/>
            <Setter Property="MinHeight" Value="22"/>
            <Setter Property="Padding" Value="6,2"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="LightBlue"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="LightBlue"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Cell text style with trimming -->
        <Style x:Key="CellTextStyle" TargetType="{x:Type TextBlock}">
            <Setter Property="Padding" Value="6,0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
            <Setter Property="Foreground" Value="#444"/>
        </Style>

        <!-- Highlight blue for ProjectStatus Open -->
        <Style x:Key="ProjectStatusStyle" TargetType="{x:Type TextBlock}" BasedOn="{StaticResource CellTextStyle}">
            <Style.Triggers>
                <DataTrigger Binding="{Binding ProjectStatus}" Value="OPEN">
                    <Setter Property="Foreground" Value="Green"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding ProjectStatus}" Value="HOLD">
                    <Setter Property="Foreground" Value="RED"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding ProjectStatus}" Value="CLOSED">
                    <Setter Property="Foreground" Value="PURPLE"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </Page.Resources>

    <Grid Margin="5">
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="10"/>
            <ColumnDefinition Width="3.2*"/>
        </Grid.ColumnDefinitions>



        <!-- Master Data Grid -->
        <control:FilterDataGrid 
            x:Name="ProjDG"
            AutoGenerateColumns="False"
            CanUserAddRows="False"
            SelectionChanged="FormProj_Sel_Changed"
            ShowRowsCount="True"
            ShowStatusBar="True"
            FilterLanguage="English"
            RowHeight="22"
            FontSize="11"
            ColumnHeaderHeight="28"
            Margin="0"
            AlternationCount="2"
            RowBackground="White"
            AlternatingRowBackground="#F8F9FB"
            ColumnHeaderStyle="{StaticResource HeaderStyle}"
            RowStyle="{StaticResource RowStyle}"
            CanUserResizeColumns="True"
            CanUserSortColumns="True"
            Grid.Column="0">
            <control:FilterDataGrid.Columns>
                <control:DataGridTextColumn Header="BOM No." Binding="{Binding PROJECTNO}" Width="110" IsColumnFiltered="True">
                    <control:DataGridTextColumn.ElementStyle>
                        <Style TargetType="{x:Type TextBlock}" BasedOn="{StaticResource CellTextStyle}">
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="Foreground" Value="Black"/>
                        </Style>
                    </control:DataGridTextColumn.ElementStyle>
                </control:DataGridTextColumn>

                <control:DataGridTextColumn Header="Customer" Binding="{Binding Customer}" Width="250" IsColumnFiltered="True" ElementStyle="{StaticResource CellTextStyle}" />

            </control:FilterDataGrid.Columns>
        </control:FilterDataGrid>

        <!-- Splitter -->
        <GridSplitter Grid.Column="1" Width="10" Background="#ddd" HorizontalAlignment="Center" VerticalAlignment="Stretch" />

        <!-- Details and StockIn Panel -->
        <Grid Grid.Column="2" Margin="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="20"/>
                <RowDefinition/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <DockPanel Grid.Row="0" HorizontalAlignment="Left">
                <!-- Details DataGrid -->
                <TextBlock Text="Project No:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" FontWeight="SemiBold" Margin="10,0,8,8"/>
                <TextBlock x:Name ="txt_Projectno" Grid.Row="0"  FontWeight="SemiBold" FontSize="15" Foreground="DarkCyan" Grid.Column="0" VerticalAlignment="Center"  Margin="0,0,8,8"/>
                <TextBlock Text="Customer Name:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" FontWeight="SemiBold" Margin="10,0,8,8"/>
                <TextBlock x:Name ="txt_Customer"  Grid.Row="0"  FontWeight="SemiBold" FontSize="15" Foreground="DarkCyan" Grid.Column="0" VerticalAlignment="Center"  Margin="0,0,8,8"/>
            </DockPanel>

            <!-- Details DataGrid -->

            <!-- Details DataGrid -->
            <control:FilterDataGrid 
    x:Name="FormDG"
    SelectionMode="Single"
    AutoGenerateColumns="False" 
                SelectionChanged="dgFORM_Sel_Changed"
    CanUserAddRows="False"
    ShowRowsCount="True"
    ShowStatusBar="True"
    FilterLanguage="English"
    RowHeight="22"
    FontSize="11"
    ColumnHeaderHeight="28"
    AlternationCount="1"
    RowBackground="White"
    AlternatingRowBackground="#F8F9FB"
    ColumnHeaderStyle="{StaticResource HeaderStyle}"
    RowStyle="{StaticResource RowStyle}"
    CanUserResizeColumns="True"
    CanUserSortColumns="True"
                Grid.Row="1"
    Margin="0,0,0,0" >

                
                
                <control:FilterDataGrid.Columns>

                    <control:DataGridTextColumn Header="BOM Name" Binding="{Binding BOMName}" Width="130" IsColumnFiltered="True">
                        <control:DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock" BasedOn="{StaticResource CellTextStyle}">
                                <Setter Property="FontWeight" Value="Bold"/>
                                <Setter Property="Foreground" Value="Black"/>
                            </Style>
                        </control:DataGridTextColumn.ElementStyle>
                    </control:DataGridTextColumn>
                    
                    <control:DataGridTextColumn Header="Part Number" Binding="{Binding PartNumber}" Width="140" IsColumnFiltered="True" ElementStyle="{StaticResource CellTextStyle}" />
                    <control:DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="280" IsColumnFiltered="True" ElementStyle="{StaticResource CellTextStyle}" />
                    <control:DataGridTextColumn Header="Length" Binding="{Binding Length}" Width="60" IsColumnFiltered="True" ElementStyle="{StaticResource CellTextStyle}" />
                    <control:DataGridTextColumn Header="Width" Binding="{Binding Width}" Width="60" IsColumnFiltered="True" ElementStyle="{StaticResource CellTextStyle}" />
                    <control:DataGridTextColumn Header="Thick" Binding="{Binding Thick}" Width="60" IsColumnFiltered="True" ElementStyle="{StaticResource CellTextStyle}" />
                    <control:DataGridTextColumn Header="Unit Weight" Binding="{Binding UnitWeight}" Width="80" IsColumnFiltered="True" ElementStyle="{StaticResource CellTextStyle}" />
                    <control:DataGridTextColumn Header="Total Weight" Binding="{Binding TotalWeight}" Width="100" IsColumnFiltered="True" ElementStyle="{StaticResource CellTextStyle}" />
                    <control:DataGridTextColumn Header="Colour" Binding="{Binding Colour}" Width="90" IsColumnFiltered="True" ElementStyle="{StaticResource CellTextStyle}" />
                    <control:DataGridTextColumn Header="Qnty" Binding="{Binding BOMQty}" Foreground="Black"  FontWeight="Bold" Width="90" IsColumnFiltered="True" ElementStyle="{StaticResource CellTextStyle}" />
                    <control:DataGridTextColumn Header="Completed" Binding="{Binding ProducedQty}" Foreground="Green"  FontWeight="SemiBold" Width="90" IsColumnFiltered="True" ElementStyle="{StaticResource CellTextStyle}" />
                    <control:DataGridTextColumn Header="Pending" Binding="{Binding BalanceQty}" Foreground="Red" FontWeight="SemiBold" Width="90" IsColumnFiltered="True" ElementStyle="{StaticResource CellTextStyle}" />
                    <control:DataGridTextColumn Header="Status" Binding="{Binding Status}" Foreground="black" FontWeight="SemiBold" Width="90" IsColumnFiltered="True" ElementStyle="{StaticResource CellTextStyle}" />
                </control:FilterDataGrid.Columns>
            </control:FilterDataGrid>





            <Grid x:Name="___Grid__3rd" Grid.Row="2">

                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto"/>


                </Grid.ColumnDefinitions>

                <control:FilterDataGrid 
                    x:Name="JobcardDG"
                   Grid.Column="1"
                    AutoGenerateColumns="False"    
                    ShowRowsCount="True"
                    CanUserAddRows="False"
                    ShowStatusBar="True"
                    FilterLanguage="English"
                    RowHeight="22"
                    FontSize="11"
                    ColumnHeaderHeight="28"
                    AlternationCount="2"
                    RowBackground="White"
                    AlternatingRowBackground="#F8F9FB"
                    ColumnHeaderStyle="{StaticResource HeaderStyle}"
                    RowStyle="{StaticResource RowStyle}"
                    CanUserResizeColumns="True"
                    CanUserSortColumns="True" Margin="0,0,10,0">

                    <control:FilterDataGrid.Columns>
                        
                        
                        <control:DataGridTextColumn Header="Lot ID" Binding="{Binding Lot_id}" Width="180" IsColumnFiltered="True" Foreground="Black"  FontWeight="Bold" ElementStyle="{StaticResource CellTextStyle}" />
                        <control:DataGridTextColumn Header="Qnty" Binding="{Binding Quantity}" Width="60" IsColumnFiltered="True" Foreground="Black"  FontWeight="Bold" ElementStyle="{StaticResource CellTextStyle}" />
                        <control:DataGridTextColumn Header="Date" Binding="{Binding Entry_date}" Width="130" IsColumnFiltered="True" ElementStyle="{StaticResource CellTextStyle}" />
                        <control:DataGridTextColumn Header="User" Binding="{Binding Done_by}" Width="100" IsColumnFiltered="True" ElementStyle="{StaticResource CellTextStyle}" />
                        <control:DataGridTextColumn Header="Moved to" Binding="{Binding Moved_to}" Width="100" IsColumnFiltered="True" ElementStyle="{StaticResource CellTextStyle}" />
                    </control:FilterDataGrid.Columns>
                </control:FilterDataGrid>

                <Border Background="White" CornerRadius="6" Padding="16" BorderBrush="#D1D9E6" BorderThickness="1" Grid.Column="2">
                    <StackPanel>
                        <TextBlock Text="Lot Details" FontWeight="SemiBold" HorizontalAlignment="Center" FontSize="13" Foreground="#222" Margin="0,0,0,12" />
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="40"/>
                                <RowDefinition Height="10"/>
                                <RowDefinition Height="25"/>
                                <RowDefinition Height="25"/>
                                <RowDefinition Height="50"/>
                                <RowDefinition Height="50"/>
                            </Grid.RowDefinitions>
                            <Button Content="Print" Grid.Row="0" Width="80" Height="30" Background="#007AFF" Foreground="White" FontWeight="SemiBold" BorderThickness="0" Cursor="Hand"  Margin="0,0,0,0"  />

                            <TextBlock Text="Lot Qty" Grid.Row="2" FontWeight="SemiBold" HorizontalAlignment="Center" FontSize="13" Foreground="#222" Margin="0,0,0,0" />
                            <TextBox x:Name="Process_combo" Grid.Row="3" FontWeight="SemiBold" FontSize="13" Width="80" Foreground="#222" Margin="0,0,0,0" />
                            <Button Content="Change" Grid.Row="4"  Width="80" Height="30" Margin="0,0,0,0" Background="#007AFF" Foreground="White" FontWeight="SemiBold" HorizontalAlignment="Center" BorderThickness="0" Cursor="Hand"  />
                            <Button Content="Cancel" Grid.Row="5"  Width="80" Height="30" Margin="0,0,0,0" Background="Red" Foreground="White" FontWeight="SemiBold" HorizontalAlignment="Center" BorderThickness="0" Cursor="Hand"  />

                        </Grid>


                    </StackPanel>
                </Border>


                <Border x:Name="DetailsTab" Background="White" CornerRadius="6" Padding="16" BorderBrush="#D1D9E6" BorderThickness="1" Margin="0,0,5,0" Grid.Column="0">
                    <StackPanel>
                        <TextBlock Text="Item Details" FontWeight="SemiBold" FontSize="13" Foreground="#222" Margin="0,0,0,12" />

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="110"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="30"/>
                                <RowDefinition Height="30"/>
                                <RowDefinition Height="30"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>



                            <TextBlock Text="Item Description :" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0,0,8,8"/>
                            <TextBlock x:Name="Txt_Description" Text="" FontSize="12" Grid.Row="1" Grid.Column="1" VerticalAlignment="Center" FontWeight="Bold" Foreground="Black" Margin="0,0,8,8"/>
                            <TextBlock Text="Required Qty :" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0,0,8,8"/>
                            <TextBlock x:Name="Txt_ReqQty" Text="" FontSize="12" Grid.Row="2" Grid.Column="1" VerticalAlignment="Center" FontWeight="Bold" Foreground="Black" Margin="0,0,8,8"/>
                            <TextBlock Text="Completed Qty :" Grid.Row="3" Grid.Column="0" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0,0,8,8"/>
                            <TextBlock x:Name="Txt_CompQty" Text="" FontSize="20" Grid.Row="3" Grid.Column="1" VerticalAlignment="Center" FontWeight="Bold" Foreground="Green" Margin="0,0,8,8"/>
                            <TextBlock Text="Balance Qty :" Grid.Row="4"  Grid.Column="0" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0,0,8,8"/>
                            <TextBlock x:Name="Txt_BalQty" Text="" FontSize="15" Grid.Row="4" Grid.Column="1" VerticalAlignment="Center" Foreground="Red" FontWeight="Bold" Margin="0,0,8,8"/>
                            <TextBlock Text="Colour :" Grid.Row="5" Grid.Column="0" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0,0,8,8"/>
                            <TextBlock x:Name="Txt_Colour" Text="" Grid.Row="5" Grid.Column="1" VerticalAlignment="Center" FontWeight="Bold" Margin="0,0,8,8"/>



                            <TextBlock Text="Lot Entry" Grid.Row="6" FontWeight="SemiBold" FontSize="13" Foreground="#222" Margin="0,0,0,12" />
                            <TextBox x:Name="Lot_qty" Grid.Row="7"  Grid.Column="0" FontWeight="SemiBold" FontSize="13" Width="80" Foreground="#222" Margin="0,0,0,0" HorizontalAlignment="Left" />
                            <Button Content="Enter" Grid.Row="7"    Grid.Column="1" Width="80" Height="30" Margin="0,0,0,0" Background="#007AFF" Foreground="White" FontWeight="SemiBold" HorizontalAlignment="Left" BorderThickness="0" Cursor="Hand" Click="LotEntry_Click"  />

                        </Grid>


                    </StackPanel>
                </Border>
            </Grid>

        </Grid>
    </Grid>
</Page>
