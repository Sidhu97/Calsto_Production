<Page
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:control="clr-namespace:FilterDataGrid;assembly=FilterDataGrid"
      xmlns:av="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="av" x:Class="Planning "
      Title="Planning" Background="#FAFAFA" FontFamily="Segoe UI" FontSize="11" av:DesignHeight="594.701">

    <Page.Resources>


        <!-- Header style -->
        <Style x:Key="HeaderStyle" TargetType="{x:Type DataGridColumnHeader}">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Background" Value="#F5F7FA"/>
            <Setter Property="Foreground" Value="#333"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="#DBE2E8"/>
        </Style>

        <!-- Row style without unconditional background -->
        <Style x:Key="RowStyle" TargetType="{x:Type DataGridRow}">
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Foreground" Value="#222"/>
            <Setter Property="MinHeight" Value="22"/>
            <Setter Property="Padding" Value="6,2"/>
            <Style.Triggers>
                <!-- Selected Row -->
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="LightBlue"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
                <!-- Hover -->
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="LightBlue"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="DataGridCell">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Foreground" 
     Value="{Binding Foreground, RelativeSource={RelativeSource AncestorType=DataGridRow}}"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="Background" Value="LightBlue"/>
                </Trigger>
            </Style.Triggers>
        </Style>


        <!-- Cell text style with trimming -->
        <Style x:Key="CellTextStyle" TargetType="{x:Type TextBlock}">
            <Setter Property="Padding" Value="6,0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
            <Setter Property="Foreground" Value="#444"/>
        </Style>

        <!-- Highlight blue for ProjectStatus Open -->
        <Style x:Key="PPCStatusStyle" TargetType="{x:Type TextBlock}" BasedOn="{StaticResource CellTextStyle}">

            <Setter Property="Foreground" Value="DarkRed"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding Status}" Value="BOM Not Available">
                    <Setter Property="Foreground" Value="LightGray"/>
                    
                </DataTrigger>
                <DataTrigger Binding="{Binding Status}" Value="PPC Released">
                    <Setter Property="Foreground" Value="Green"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                </DataTrigger>
                
            </Style.Triggers>
        </Style>
    </Page.Resources>

    <Grid Margin="0,5,0,0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="5"/>
            <ColumnDefinition Width="3.2*"/>
        </Grid.ColumnDefinitions>

        

        <!-- Master Data Grid -->
        <control:FilterDataGrid 
            x:Name="ProjNoDG"
            AutoGenerateColumns="False"
            CanUserAddRows="False"
            SelectionChanged="dgProj_Sel_Changed"
            ShowRowsCount="True"
            ShowStatusBar="True"
            FilterLanguage="English"
            RowHeight="22"
            FontSize="11"
            ColumnHeaderHeight="28"
            Margin="0"
            AlternationCount="2"
            RowBackground="White"
            AlternatingRowBackground="#F8F9FB"
            ColumnHeaderStyle="{StaticResource HeaderStyle}"
            RowStyle="{StaticResource RowStyle}"
            CanUserResizeColumns="True"
            CanUserSortColumns="True"
            Grid.Column="0">
            <control:FilterDataGrid.Columns>
                <control:DataGridTextColumn Header="BOM No." Binding="{Binding PROJECTNO}" Width="80" IsColumnFiltered="True">
                    <control:DataGridTextColumn.ElementStyle>
                        <Style TargetType="{x:Type TextBlock}" BasedOn="{StaticResource CellTextStyle}">
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="Foreground" Value="Black"/>
                        </Style>
                    </control:DataGridTextColumn.ElementStyle>
                </control:DataGridTextColumn>

                <control:DataGridTextColumn Header="Customer" Binding="{Binding Customer}" Width="200" IsColumnFiltered="True" ElementStyle="{StaticResource CellTextStyle}" />
                <control:DataGridTextColumn Header="PPC Status" Binding="{Binding Status}"  Width="130" IsColumnFiltered="True" ElementStyle="{StaticResource PPCStatusStyle}" />
            </control:FilterDataGrid.Columns>
        </control:FilterDataGrid>

        <!-- Splitter -->
        <GridSplitter Grid.Column="1" Width="5" Background="#ddd" HorizontalAlignment="Center" VerticalAlignment="Stretch" />

        <!-- Details and StockIn Panel -->
        <Grid Grid.Column="2" Margin="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="20"/>
                <RowDefinition/>
                <RowDefinition Height="140"/>
            </Grid.RowDefinitions>

                      
            <!-- Details DataGrid -->
           
            <Grid Grid.Row="1" Grid.Column="0" Margin="0">

                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition Width="Auto"/>
                    
                </Grid.ColumnDefinitions>

                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <DockPanel Grid.Row="0" Margin="0,0,0,0" >
                    <!-- Details DataGrid -->
                    <TextBlock Text="Project No:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" FontWeight="SemiBold" Margin="10,0,8,8"/>
                    <TextBlock x:Name ="txt_Projectno" Grid.Row="0"  FontWeight="SemiBold" FontSize="15" Foreground="DarkCyan" Grid.Column="0" VerticalAlignment="Center"  Margin="0,0,8,8"/>
                    <TextBlock Text="Customer Name:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" FontWeight="SemiBold" Margin="10,0,8,8"/>

                    <TextBlock x:Name ="txt_Customer"  FontWeight="SemiBold" FontSize="15" Foreground="DarkCyan" Grid.Column="0" VerticalAlignment="Center"  Margin="0,0,8,8"/>
                    


                </DockPanel>

                <CheckBox x:Name="SelectAllFilteredCheckBox" Grid.Row="0" Grid.Column="1" Content="Select all Item" DockPanel.Dock="Right" Width="130" VerticalAlignment="Center" Checked="BtnSelectAll_Click" Unchecked="BtnClearSelection_Click" Margin="0,0,0,0"/>
                <!-- Details DataGrid -->
            <control:FilterDataGrid 
    x:Name="WIDBOMDG"
    Grid.Column="0"
    SelectionMode="Single"
    SelectionChanged="dgWId_Sel_Changed"
    AutoGenerateColumns="False" 
    CanUserAddRows="False"
    ShowRowsCount="True"
    ShowStatusBar="True"
    FilterLanguage="English"
    RowHeight="22"
    FontSize="11"
    ColumnHeaderHeight="28"
    AlternationCount="1"
    RowBackground="White"
    AlternatingRowBackground="#F8F9FB"
    ColumnHeaderStyle="{StaticResource HeaderStyle}"
    RowStyle="{StaticResource RowStyle}"
    CanUserResizeColumns="True"
    CanUserSortColumns="True"
    Margin="0,0,0,12" Grid.Row="1">

                <control:FilterDataGrid.Columns>
                        
                        <control:DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="300" IsColumnFiltered="True" ElementStyle="{StaticResource CellTextStyle}" />
                    <control:DataGridTextColumn Header="Assigned" Binding="{Binding Assigned}" Width="110" IsColumnFiltered="True" ElementStyle="{StaticResource CellTextStyle}" />
                    <control:DataGridTextColumn Header="BOM Qty" Binding="{Binding BOMQty}" Width="110" IsColumnFiltered="True" ElementStyle="{StaticResource CellTextStyle}" />
                    <control:DataGridTextColumn Header="Process" Binding="{Binding Process}" Width="100" IsColumnFiltered="True" ElementStyle="{StaticResource CellTextStyle}" />
                    <control:DataGridTextColumn Header="Colour" Binding="{Binding Colour}" Width="90" IsColumnFiltered="True" ElementStyle="{StaticResource CellTextStyle}" />
                    <control:DataGridTextColumn Header="Packing Balance" Binding="{Binding PackingBalance}" Width="90" IsColumnFiltered="True" ElementStyle="{StaticResource CellTextStyle}" />
                    <control:DataGridTextColumn Header="Dispatched Qty" Binding="{Binding DispatchQty}" Width="90" IsColumnFiltered="True" ElementStyle="{StaticResource CellTextStyle}" />
                    <control:DataGridTextColumn Header="Dispatch Date" Binding="{Binding DispatchDate, StringFormat=\{0:dd-MM-yyyy\}}" Width="110" IsColumnFiltered="True" ElementStyle="{StaticResource CellTextStyle}" />
                    <control:DataGridTextColumn Header="Dispatch Pending" Binding="{Binding DispatchPending}" Width="90" IsColumnFiltered="True" />
                        <control:DataGridTextColumn Header="Job" Binding="{Binding Jobcard}" Width="40" IsColumnFiltered="True" />

                        <control:DataGridTemplateColumn Header="Create Job" Width="80">
                            <control:DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <CheckBox 
                Content="Job Card"
                VerticalAlignment="Center"
                Padding="2,2"
                IsChecked="{Binding IsSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                                        <CheckBox.Style>
                                            <Style TargetType="CheckBox">
                                                <Setter Property="IsEnabled" Value="True"/>
                                                
                                                
                                                
                                                <Style.Triggers>
                                                    <!-- Disable when Jobcard = Y -->
                                                    <DataTrigger Binding="{Binding Jobcard}" Value="Y">
                                                        <Setter Property="IsEnabled" Value="False"/>
                                                       
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </CheckBox.Style>
                                    </CheckBox>
                                </DataTemplate>
                            </control:DataGridTemplateColumn.CellTemplate>
                        </control:DataGridTemplateColumn>


                    </control:FilterDataGrid.Columns>
            </control:FilterDataGrid>


                <Border Background="White" Grid.Row="1" CornerRadius="6" Padding="16" BorderBrush="#D1D9E6" BorderThickness="1" Margin="0,0,0,12" Grid.Column="1">
                    <StackPanel>
                        <TextBlock Text="Job Cards" FontWeight="SemiBold" HorizontalAlignment="Center" FontSize="13" Foreground="#222" Margin="0,0,0,12" />

                        
                        
                        <Grid>
                            
                            <Grid.RowDefinitions>
                                <RowDefinition Height="40"/>
                                <RowDefinition Height="100"/>
                                <RowDefinition Height="25"/>
                                <RowDefinition Height="25"/>
                                <RowDefinition Height="50"/>
                                
                               
                            </Grid.RowDefinitions>

                            
                            
                            <Button Content="Create Jobs" Grid.Row="0" Width="80" Height="30" Background="#007AFF" Foreground="White" FontWeight="SemiBold" BorderThickness="0" Cursor="Hand"  Margin="0,0,0,0" Click="CreateJobButton_Click" />
                            <TextBlock Text="For Selected Items" Grid.Row="1"  FontWeight="SemiBold" FontSize="10" HorizontalAlignment="Center" Foreground="#222" Margin="0,0,0,0" />
                            <TextBlock Text="Process Change" Grid.Row="2" FontWeight="SemiBold" HorizontalAlignment="Center" FontSize="13" Foreground="#222" Margin="0,0,0,0" />
                            <ComboBox x:Name="Process_combo" Grid.Row="3" FontWeight="SemiBold" FontSize="13" Width="80" Foreground="#222" Margin="0,0,0,0" />
                            <Button Content="Apply" Grid.Row="4"  Width="80" Height="30" Margin="0,0,0,0" Background="#007AFF" Foreground="White" FontWeight="SemiBold" HorizontalAlignment="Center" BorderThickness="0" Cursor="Hand"  />
                            

                        </Grid>


                    </StackPanel>
                </Border>


            </Grid>

            <Grid x:Name="___Grid__3rd" Grid.Row="2">

                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition Width="auto" />

                </Grid.ColumnDefinitions>

                <control:FilterDataGrid 
                    x:Name="JobcardDG"
                   
                    AutoGenerateColumns="False"    
                    ShowRowsCount="True"
                    CanUserAddRows="False"
                    ShowStatusBar="True"
                    FilterLanguage="English"
                    RowHeight="22"
                    FontSize="11"
                    ColumnHeaderHeight="28"
                    AlternationCount="2"
                    RowBackground="White"
                    AlternatingRowBackground="#F8F9FB"
                    ColumnHeaderStyle="{StaticResource HeaderStyle}"
                    RowStyle="{StaticResource RowStyle}"
                    CanUserResizeColumns="True"
                    CanUserSortColumns="True" Margin="0,0,0,0">

                    <control:FilterDataGrid.Columns>
                        <control:DataGridTextColumn Header="Seq." Binding="{Binding OP_Sequence}" Width="60" IsColumnFiltered="True" ElementStyle="{StaticResource CellTextStyle}" />
                        <control:DataGridTextColumn Header="Job Card" Binding="{Binding JC_no}" Width="150" IsColumnFiltered="True" ElementStyle="{StaticResource CellTextStyle}" />
                        <control:DataGridTextColumn Header="WID" Binding="{Binding WID}" Width="100" IsColumnFiltered="True" ElementStyle="{StaticResource CellTextStyle}" />
                        <control:DataGridTextColumn Header="Operation" Binding="{Binding Operation_ID}" Width="100" IsColumnFiltered="True" ElementStyle="{StaticResource CellTextStyle}" />
                        <control:DataGridTextColumn Header="Created On" Binding="{Binding Created_date, StringFormat=\{0:dd-MM-yyyy\}}" Width="100" IsColumnFiltered="True" ElementStyle="{StaticResource CellTextStyle}" />
                        <control:DataGridTextColumn Header="Created by" Binding="{Binding Created_by}" Width="100" IsColumnFiltered="True" ElementStyle="{StaticResource CellTextStyle}" />
                        <control:DataGridTextColumn Header="Available" Binding="{Binding Ready_qty}" Width="100" IsColumnFiltered="True" ElementStyle="{StaticResource CellTextStyle}" />
                        <control:DataGridTextColumn Header="Completed" Binding="{Binding Job_Qty}" Width="100" IsColumnFiltered="True" ElementStyle="{StaticResource CellTextStyle}" />
                    </control:FilterDataGrid.Columns>
                </control:FilterDataGrid>
                <Button Content="Delete Jobs" Grid.Column="1"  Margin="0,0,0,0" Background="red" Foreground="White" FontWeight="SemiBold" HorizontalAlignment="Left" BorderThickness="0" Cursor="Hand" Click="DeleteJobButton_Click"/>
            </Grid>

        </Grid>
    </Grid>
</Page>
